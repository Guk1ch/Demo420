<Page x:Class="SochnevDemoApp.Pages.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SochnevDemoApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="ProductListPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Поиск: "/>
                    <TextBox Name="SearchTb"
                         Width="450" TextChanged="SearchTb_TextChanged"/>
                </StackPanel>
                <StackPanel
                          Margin="40 0">
                    <TextBlock Text="Сортировка: "/>
                    <ComboBox Name="SortCb"
                              SelectionChanged="SortCb_SelectionChanged"
                          Width="600">
                        <ComboBoxItem Content="Наименование по возрастанию"/>
                        <ComboBoxItem Content="Наименование по убыванию"/>
                        <ComboBoxItem Content="Номер производственного цеха по возрастанию"/>
                        <ComboBoxItem Content="Номер производственного цеха по убыванию"/>
                        <ComboBoxItem Content="Минимальная стоимость для агента по возрастанию"/>
                        <ComboBoxItem Content="Минимальная стоимость для агента по убыванию"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel
                          Margin="10 0">
                    <TextBlock Text="Фильтрация: "/>
                    <ComboBox Name="FilterCb"
                              SelectionChanged="FilterCb_SelectionChanged"
                          Width="500"/>
                </StackPanel>
            </StackPanel>
            <ListView Name="ProductList"
                    Margin="0 10 0 0"
                      Width="1700"
                      Height="800"
                      BorderThickness="0"
                      HorizontalContentAlignment="Stretch" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionChanged="ProductList_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black"
                                Background="#D3D3D3"
                                BorderThickness="0.5">
                            <Grid Margin="0 20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="240"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding BindingPath}"
                                       Grid.Column="0"
                                       Width="200"
                                       Height="200"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontSize="30">
                                        <Run Text="{Binding ProductType.Title}"/>
                                        <Run Text="|"/>
                                        <Run Text="{Binding Title}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding ArticleNumber}"/>
                                    <ListView ItemsSource="{Binding MaterialList}"
                                              Background="Transparent"
                                              HorizontalAlignment="Left"
                                              BorderThickness="0">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding .}"/>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                        <ListView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"
                                                           MaxWidth="950"/>
                                            </ItemsPanelTemplate>
                                        </ListView.ItemsPanel>
                                    </ListView>
                                </StackPanel>
                                <TextBlock Text="{Binding TotalCost}"
                                           HorizontalAlignment="Center"
                                           Grid.Column="2"/>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Name="AddBtn"
                    Content="Добавить продукцию"
                    Width="300"
                    Margin="0 10 0 0" Click="AddBtn_Click"/>
        </StackPanel>
    </Grid>
</Page>
